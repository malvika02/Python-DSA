import random

def mergeSort(arr):
    # Store the length of the list
    arrlength = len(arr)
    # list with length less than is already sorted
    if arrlength == 1:
        return arr
    # identify the list midpoint and partition the list into a left partition 






















# def MergeSort(arr):
#     for i in range(len(arr)):
#         m = len(arr)/2
#         L = arr[:m]
#         R = arr[m+1:]

#     return m
# m = [12,32,43,41,33,1,9]
# print(m)


            